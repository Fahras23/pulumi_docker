config:
  provider:backend_port: "3000"
  provider:database: cart
  provider:frontend_port: "3001"
  provider:mongo_host: mongodb://mongo:27017
  provider:mongo_port: "27017"
  provider:node_enviroment: development
  provider:protocol: http://
